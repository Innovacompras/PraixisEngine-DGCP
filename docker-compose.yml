services:
  api:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - chroma_data:/app/chroma_data
      - ./logs:/app/logs
    restart: unless-stopped

volumes:
  chroma_data:
